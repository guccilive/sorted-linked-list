<?xml version="1.0"?>
<ruleset name="SortedLinkedList">
    <description>Coding standard for Sorted Linked List library</description>
    
    <!-- Show progress -->
    <arg value="p"/>
    
    <!-- Use colors -->
    <arg name="colors"/>
    
    <!-- Check both src and tests -->
    <file>src</file>
    <file>tests</file>
    
    <!-- PSR-12 as base standard -->
    <rule ref="PSR12"/>
    
    <!-- Slevomat Coding Standard -->
    
    <!-- Require strict types declaration -->
    <rule ref="SlevomatCodingStandard.TypeHints.DeclareStrictTypes">
        <properties>
            <property name="spacesCountAroundEqualsSign" value="0"/>
        </properties>
    </rule>
    
    <!-- Type hints -->
    <rule ref="SlevomatCodingStandard.TypeHints.ParameterTypeHint"/>
    <rule ref="SlevomatCodingStandard.TypeHints.PropertyTypeHint"/>
    <rule ref="SlevomatCodingStandard.TypeHints.ReturnTypeHint"/>
    
    <!-- Union type hint format -->
    <rule ref="SlevomatCodingStandard.TypeHints.UnionTypeHintFormat"/>
    
    <!-- Null type hint on last position in annotations -->
    <rule ref="SlevomatCodingStandard.TypeHints.NullTypeHintOnLastPosition"/>
    
    <!-- Require use of null coalesce operator when possible -->
    <rule ref="SlevomatCodingStandard.ControlStructures.RequireNullCoalesceOperator"/>
    
    <!-- Yoda conditions -->
    <rule ref="SlevomatCodingStandard.ControlStructures.DisallowYodaComparison"/>
    
    <!-- Namespace and use statements -->
    <rule ref="SlevomatCodingStandard.Namespaces.UnusedUses">
        <properties>
            <property name="searchAnnotations" value="true"/>
        </properties>
    </rule>
    <rule ref="SlevomatCodingStandard.Namespaces.UseFromSameNamespace"/>
    <rule ref="SlevomatCodingStandard.Namespaces.UselessAlias"/>
    
    <!-- Classes -->
    <rule ref="SlevomatCodingStandard.Classes.ClassConstantVisibility"/>
    <rule ref="SlevomatCodingStandard.Classes.ModernClassNameReference"/>
    
    <!-- Comments -->
    <rule ref="SlevomatCodingStandard.Commenting.EmptyComment"/>
    
    <!-- Arrays -->
    <rule ref="SlevomatCodingStandard.Arrays.TrailingArrayComma"/>
</ruleset>
